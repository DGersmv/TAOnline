<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Разрешения на камеру и доступ к файлам -->
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE"/>
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />



    <!-- Для Android 10+ WRITE_EXTERNAL_STORAGE не обязателен, но добавим -->
    <uses-permission android:name="android.permission.ACCESS_MEDIA_LOCATION"/>
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

    <!-- Разрешение на доступ в интернет -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- ✅ Фикс ошибки: указываем, что камера не обязательна -->
    <uses-feature android:name="android.hardware.camera" android:required="false"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TAOnline"
        tools:targetApi="31">

        <!-- Экран авторизации (LoginActivity) теперь основной -->
        <activity
            android:name=".LoginActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.TAOnline">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            
            <!-- Deep link для загрузки (админ) -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="tashi-ani" android:host="upload" />
            </intent-filter>
            
            <!-- Deep link для просмотра (заказчик) -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="tashi-ani" android:host="view" />
            </intent-filter>
        </activity>

        <!-- Основной экран (MainActivity), но теперь он не стартовый -->
        <activity android:name=".MainActivity" android:exported="true"/>

        <!-- Экран для работы с фото -->
        <activity android:name=".PhotoActivity" android:exported="true"/>

        <!-- Activity для съемки 360 панорам -->
        <activity android:name=".Panorama360Activity" android:exported="false"/>

        <!-- 📌 Добавляем `UploadActivity` -->
        <activity android:name=".UploadActivity" android:exported="true"/>
        
        <!-- Экран просмотра фото для заказчика -->
        <activity android:name=".PhotoViewActivity" android:exported="true"/>
        
        <!-- Экран выбора заказчиков (админ) -->
        <activity android:name=".CustomersActivity" android:exported="true"/>
        
        <!-- Экран объектов заказчика (админ) -->
        <activity android:name=".CustomerObjectsActivity" android:exported="true"/>
        
        <!-- Экран списка объектов для заказчика -->
        <activity android:name=".CustomerObjectsListActivity" android:exported="true"/>
        
        <!-- Экран выбора папок объекта для заказчика -->
        <activity android:name=".ObjectFoldersActivity" android:exported="true"/>

        <!-- 📌 Настройки `FileProvider` для работы с фото -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:grantUriPermissions="true"
            android:exported="false">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths" />
        </provider>

    </application>

</manifest>
