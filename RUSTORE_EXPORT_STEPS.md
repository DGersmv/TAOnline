# 📋 RuStore - Экспорт подписи приложения

## Пошаговая инструкция по экспорту ключа для RuStore

---

## Шаг 1: Скачать инструмент PEPK ✅

1. Нажмите кнопку **"Скачать"** в RuStore (шаг 1)
2. Сохраните файл `pepk.jar` в удобное место (например, `C:\Рабочая\Android\TAOnline\`)

---

## Шаг 2: Запустить инструмент с помощью команды

### Нужно узнать:
1. **Путь к keystore** - уже знаем: `C:\Users\count\AndroidStudioProjects\TAOnline\taonline-release-key.jks`
2. **Алиас** - нужно узнать через keytool

### Сначала узнаем алиас:

Запустите скрипт `find-keytool-and-check.bat` который я создал, или выполните команду:

```batch
cd C:\Users\count\AndroidStudioProjects\TAOnline
```

Затем найдите keytool (обычно в Android Studio):
```batch
"C:\Program Files\Android\Android Studio\jbr\bin\keytool.exe" -list -keystore taonline-release-key.jks
```

Введите пароль и скопируйте алиас (обычно это `taonline-key` или `taonline-release-key`)

### Затем выполните команду экспорта:

```batch
cd C:\Рабочая\Android\TAOnline

java -jar pepk.jar --keystore "C:\Users\count\AndroidStudioProjects\TAOnline\taonline-release-key.jks" --alias ВАШ_АЛИАС --output pepk_out.zip --encryptionkey=00006b84f2e831c8118adb39ec405e1618ec9953ff3ad759dbf5c07a3095abf65531928a9fe49be549e9acfc55462fc44f5ebc28224cd951c41326edc88bc31e4022161f --include-cert
```

**Замените `ВАШ_АЛИАС`** на правильный алиас (из шага выше)

Введите пароль keystore когда попросит.

---

## Шаг 3: Загрузить созданный ZIP-архив

1. После успешного экспорта будет создан файл `pepk_out.zip`
2. Нажмите **"Выберите файл"** в RuStore (шаг 3)
3. Выберите файл `pepk_out.zip` (должен быть не более 100 KB)

---

## Шаг 4: Загрузить сертификат загрузки

Сертификат PEM обычно создается автоматически при экспорте, или нужно его извлечь из keystore.

### Создание PEM сертификата:

```batch
keytool -export -rfc -keystore "C:\Users\count\AndroidStudioProjects\TAOnline\taonline-release-key.jks" -alias ВАШ_АЛИАС -file upload_certificate.pem
```

1. Нажмите **"Выберите файл"** в RuStore (шаг 4)
2. Выберите файл `upload_certificate.pem`

---

## Шаг 5: Отправить подпись

После загрузки обоих файлов нажмите **"Отправить подпись"**

---

## ⚠️ Важно:

- Сохраните keystore и пароли - они нужны для будущих обновлений
- Файлы должны быть не более 100 KB каждый
- Используйте правильный алиас из вашего keystore


